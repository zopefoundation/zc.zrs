[buildout]
develop = tests
parts = test-eval
eggs-directory = eval/dist
download-cache = 
installed = .eval-installed.cfg

[test-eval]
recipe = zc.recipe.testrunner
eggs = zc.zrstests
initialization = 
  import os, tempfile
  try: os.mkdir('tmp')
  except: pass
  tempfile.tempdir = os.path.abspath('tmp')
defaults = ['--all']
